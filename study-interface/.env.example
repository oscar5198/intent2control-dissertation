# Example local configuration for development only.
# Real credentials, secrets, database URLs, and export tokens must never be committed.

APP_ENV=development
APP_URL=http://localhost:8000
DATABASE_URL=
ADMIN_EXPORT_TOKEN=
SESSION_SECRET=
